// 1. Create a functional React component that renders a greeting message.
//  The component should accept a name prop and display "Hello, {name}!".

export default function Greeting({name})
{
    return(
        <h1>Greetings, {name}</h1>
    );
}


